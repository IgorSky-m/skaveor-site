server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IgorSky-m/training-diary-config
          clone-on-start: true
          username: ${GIT_USER}
          password: ${GIT_TOKEN}
          default-label: "main"
